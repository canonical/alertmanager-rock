summary: Run goss tests

environment:
  GOSS_FILE: "../../../goss.yaml"

execute: |
  storage_dir=$(mktemp -d)
  trap 'rm -rf "$storage_dir"' EXIT
  chown 584792:584792 "$storage_dir"
  dgoss run -v "$storage_dir:/alertmanager" rockcraft-test:latest

